const content = {
  basic: [
    "अ आ इ ई उ ऊ ऋ ए ऐ ओ औ अं अः",
    "क ख ग घ ङ च छ ज झ ञ ट ठ ड ढ ण",
    "त थ द ध न प फ ब भ म य र ल व श ष स ह"
  ],
  intermediate: [
    "भारत मेरा देश है। आज मौसम बहुत अच्छा है। हमें रोज व्यायाम करना चाहिए।",
    "सफलता के लिए मेहनत जरूरी है। आम फल घर चल बस कर नल जल धन मन।",
    "किताब चिड़िया डाकिया रविवार किसान बगीचा तितली कहानी मछली रानी।",
    "भारत एक महान देश है। यहाँ की संस्कृति बहुत पुरानी है। हमें अपने स्वास्थ्य का ध्यान रखना चाहिए।",
    "शिक्षा केवल साक्षरता का नाम नहीं है, बल्कि यह मनुष्य के व्यक्तित्व का सर्वांगीण विकास करने का एक सशक्त माध्यम है। एक शिक्षित समाज ही राष्ट्र की प्रगति में अपना बहुमूल्य योगदान दे सकता है। आज के प्रतिस्पर्धी युग में केवल किताबी ज्ञान पर्याप्त नहीं है, बल्कि तकनीकी कौशल का होना भी अनिवार्य हो गया है। विद्यालयों और विश्वविद्यालयों को चाहिए कि वे विद्यार्थियों में नैतिक मूल्यों का बीजारोपण करें ताकि वे भविष्य में जिम्मेदार नागरिक बन सकें। अनुशासन और परिश्रम सफलता की दो मुख्य कुंजियाँ हैं। यदि हम अपने समय का सदुपयोग करना सीख जाएं, तो कोई भी लक्ष्य प्राप्त करना असंभव नहीं होगा। प्राचीन काल में भारत विश्व गुरु कहलाता था क्योंकि हमारी शिक्षा पद्धति में संस्कार और विज्ञान का अद्भुत संगम था। हमें पुनः उसी गौरव को प्राप्त करने के लिए सामूहिक प्रयास करने होंगे।",
    "प्राकृतिक संसाधनों का अंधाधुंध दोहन हमारे पर्यावरण के लिए एक बड़ा खतरा बन गया है। बढ़ता हुआ प्रदूषण और ग्लोबल वार्मिंग जैसी समस्याएँ आज पूरी दुनिया के सामने चुनौती बनकर खड़ी हैं। यदि हमने समय रहते वृक्षारोपण और जल संरक्षण की दिशा में ठोस कदम नहीं उठाए, तो आने वाली पीढ़ियों को भारी संकट का सामना करना पड़ेगा। सौर ऊर्जा और पवन ऊर्जा जैसे वैकल्पिक स्रोतों का उपयोग बढ़ाना अब हमारी मजबूरी नहीं, बल्कि आवश्यकता बन गई है। नदियों को स्वच्छ रखना और प्लास्टिक का उपयोग कम करना हम सभी का नैतिक दायित्व है। जीव-जंतुओं और वनस्पतियों की रक्षा करना ही वास्तव में मानवता की सेवा है। प्रकृति हमें निस्वार्थ भाव से सब कुछ देती है, बदले में हमें केवल उसका सम्मान और संरक्षण करना चाहिए। प्रदूषण मुक्त वातावरण ही स्वस्थ जीवन का आधार है।",
    "भारत अपनी सांस्कृतिक विविधता और ऐतिहासिक धरोहरों के लिए संपूर्ण विश्व में विख्यात है। कश्मीर से कन्याकुमारी तक फैली इस पावन धरा पर अनेक दर्शनीय स्थल विद्यमान हैं। विदेशी पर्यटकों के लिए भारतीय स्थापत्य कला और प्राचीन मंदिर सदैव ही आकर्षण का केंद्र रहे हैं। पर्यटन उद्योग न केवल राजस्व प्राप्ति का प्रमुख स्रोत है, बल्कि यह स्थानीय कलाकारों और शिल्पकारों को रोजगार के अवसर भी प्रदान करता है। वाराणसी के घाट, राजस्थान के दुर्ग और दक्षिण भारत के भव्य मंदिर हमारी गौरवशाली विरासत के जीवंत प्रतीक हैं। सरकार द्वारा 'स्वदेश दर्शन' जैसी योजनाओं के माध्यम से आधारभूत सुविधाओं का विस्तार किया जा रहा है। हमें अपनी प्राचीन सभ्यता के संरक्षण हेतु संकल्प लेना चाहिए और आने वाली पीढ़ी को इसके महत्व से अवगत कराना चाहिए। आतिथ्य सत्कार की हमारी 'अतिथि देवो भव' की परंपरा ही हमें विश्व के अन्य देशों से विशिष्ट बनाती है।"
  ],
  advance: [
    "सूचना प्रौद्योगिकी के इस युग में कंप्यूटर साक्षरता अत्यंत आवश्यक है। सरकारी कार्यालयों में अब अधिकांश कार्य डिजिटल माध्यम से संपन्न किए जा रहे हैं।",
    "भारत सरकार द्वारा लागू की गई नई शिक्षा नीति का उद्देश्य छात्रों का सर्वांगीण विकास करना है। इसके अंतर्गत कौशल विकास पर विशेष बल दिया गया है।",
    "अधोहस्ताक्षरी को यह सूचित करने का निर्देश हुआ है कि आगामी सोमवार को प्रशासनिक भवन के सभागार में एक आवश्यक बैठक आयोजित की जाएगी।",
    "डिजिटल इंडिया का सपना तभी साकार होगा जब गाँव-गाँव तक तकनीक पहुँचेगी। विज्ञान और प्रौद्योगिकी के क्षेत्र में भारत ने असाधारण प्रगति की है।",
    "भारतीय लोकतंत्र की आधारशिला इसकी संवैधानिक संस्थाओं की स्वायत्तता और शुचिता पर टिकी हुई है। वर्तमान परिप्रेक्ष्य में, वैश्वीकरण के इस युग में सूचना प्रौद्योगिकी ने हमारे जीवन के प्रत्येक दृष्टिकोण को परिवर्तित कर दिया है। जहाँ एक ओर कृत्रिम बुद्धिमत्ता ने विकास के नए द्वार खोले हैं, वहीं दूसरी ओर साइबर सुरक्षा और व्यक्तिगत डेटा की गोपनीयता जैसी चुनौतियाँ हमारे समक्ष खड़ी हैं। राष्ट्र के सर्वांगीण विकास हेतु यह अनिवार्य है कि हम अपनी सांस्कृतिक विरासत और आधुनिक वैज्ञानिक प्रगति के मध्य एक सुदृढ़ संतुलन स्थापित करें। संवेदनशीलता और उत्तरदायित्व के साथ कार्य करना ही एक आदर्श नागरिक की पहचान है। भ्रष्टाचार के उन्मूलन के लिए समाज के प्रत्येक वर्ग को अपनी संकीर्ण मानसिकता का परित्याग कर राष्ट्रहित को सर्वोपरि रखना होगा। तभी हम एक सशक्त, समृद्ध और आत्मनिर्भर भारत की संकल्पना को साकार कर सकेंगे।",
    "न्यायालय के क्षेत्राधिकार के अंतर्गत विचाराधीन मामलों में साक्ष्यों की सूक्ष्म विवेचना करना अनिवार्य होता है। प्राकृतिक आपदाओं के प्रबंधन हेतु राज्य सरकारों को त्वरित कार्रवाई बल की उपलब्धता सुनिश्चित करनी चाहिए। पारिस्थितिकी तंत्र में असंतुलन के कारण भूमंडलीय तापन की समस्या विकराल रूप धारण कर रही है, जिसके परिणामस्वरूप हिमनदों का पिघलना और समुद्र के जलस्तर में निरंतर वृद्धि होना चिंताजनक है। औद्योगिक इकाइयों द्वारा उत्सर्जित प्रदूषकों का वैज्ञानिक निस्तारण न होना पर्यावरण के लिए अत्यंत घातक सिद्ध हो रहा है। इसके अतिरिक्त, मानव संसाधन विकास मंत्रालय द्वारा कार्यान्वित की जा रही नई शिक्षा नीति में भाषाई विविधता और कौशल उन्नयन पर विशेष बल दिया गया है। स्वावलंबन और उद्यमिता को प्रोत्साहन देकर ही हम बेरोजगारी की समस्या का स्थायी समाधान खोज सकते हैं। संस्तुति की गई है कि भविष्य में सभी परियोजनाओं का क्रियान्वयन पारदर्शी तरीके से किया जाए।",
    "मानवीय मूल्यों का ह्रास वर्तमान सभ्यता के लिए एक गंभीर चुनौती बन चुका है। पाश्चात्य संस्कृति के अंधानुकरण ने हमारी मौलिक अस्मिता को संकट में डाल दिया है। सहिष्णुता, भ्रातृत्व और परोपकार जैसी उदात्त भावनाएँ अब केवल पुस्तकीय ज्ञान तक सीमित रह गई हैं। भौतिकवादी दृष्टिकोण ने मनुष्य को यंत्रवत बना दिया है, जिससे अंतर्द्वंद्व और मानसिक संताप में निरंतर वृद्धि हो रही है। अध्यात्म और विज्ञान के समन्वय से ही आत्मिक शांति की प्राप्ति संभव है। साहित्यकार समाज का दर्पण होता है, जो अपनी लेखनी के माध्यम से जनमानस की सुप्त चेतना को जाग्रत करने का दुष्कर कार्य करता है। छंदबद्ध रचनाओं और गद्य की क्लिष्ट विधाओं में निपुणता प्राप्त करना किसी भी साधक के लिए धैर्य की परीक्षा है। अतएव, हमें अपनी वैचारिक संकीर्णता का त्याग कर सार्वभौमिक कल्याण की दिशा में अग्रसर होना चाहिए।",
    "राजकोषीय घाटे को नियंत्रित करने हेतु वित्त मंत्रालय द्वारा कड़े सुधारात्मक कदम उठाए जा रहे हैं। प्रत्यक्ष विदेशी निवेश (FDI) की सुलभता ने विनिर्माण क्षेत्र में अभूतपूर्व परिवर्तन किए हैं। न्यायपालिका की सक्रियता से जनहित याचिकाओं के निस्तारण में तेजी आई है, जिससे सामान्य नागरिकों का संवैधानिक व्यवस्था में विश्वास सुदृढ़ हुआ है। किसी भी संविदा (Contract) के उल्लंघन की स्थिति में पीड़ित पक्ष को क्षतिपूर्ति का विधिक अधिकार प्राप्त है। दिवाला और शोधन अक्षमता संहिता के प्रावधानों के तहत ऋणदाताओं के हितों का संरक्षण सुनिश्चित किया गया है। अधोसंरचना विकास परियोजनाओं के क्रियान्वयन में पारदर्शिता और जवाबदेही अनिवार्य है। सार्वजनिक उपक्रमों के विनिवेश की प्रक्रिया में श्रमिक संगठनों के सुझावों को सम्मिलित करना लोकतांत्रिक मर्यादा के अनुकूल है। सांख्यिकीय आंकड़ों के विश्लेषण से यह स्पष्ट है कि मुद्रास्फीति की दर में उतार-चढ़ाव का सीधा प्रभाव उपभोक्ता की क्रय शक्ति पर पड़ता है।"
  ]
};

let currentMode = "practicing";
let currentLevel = "intermediate";
let targetText = "";
let startTime = null;
let timerInterval = null;
let errors = 0;
let timeLeft = 0;
let lastOffsetTop = 0;
let initialTimeLimit = 0;
let typingFontSize = 20;
let typingFontWeight = "400";
let typingFontStyle = "normal";
let currentWordIndex = 0;
let backspaceCount = 0;
let correctWordsCount = 0;
let incorrectWordsCount = 0;
let totalWordsCount = 0;

const displayEl = document.getElementById("display-text");
const inputEl = document.getElementById("input-area");
const wpmEl = document.getElementById("wpm");
const accuracyEl = document.getElementById("accuracy");
const timerEl = document.getElementById("timer");
const setupScreenEl = document.getElementById("setup-screen");
const typingScreenEl = document.getElementById("typing-screen");
const resultScreenEl = document.getElementById("result-screen");

function init() {
  populateParaSelect();
  resetSession();
  updateHistoryTable();
  applyTypingFontStyle();
  updateRings();
}

function startTyping() {
  resetSession(true);
  showScreen("typing");
  inputEl.disabled = false;
  inputEl.focus();
}

function showScreen(screenName) {
  const screens = [setupScreenEl, typingScreenEl, resultScreenEl];

  screens.forEach((screen) => {
    screen.classList.remove("is-visible");
    screen.classList.add("is-hidden");
  });

  const activeScreen =
    screenName === "typing" ? typingScreenEl : screenName === "result" ? resultScreenEl : setupScreenEl;

  activeScreen.classList.remove("is-hidden");
  activeScreen.classList.add("is-visible");
  document.body.classList.add("body-ready");
}

function showSetup() {
  showScreen("setup");
}

function showTyping() {
  showScreen("typing");
}

function applyTypingFontStyle() {
  inputEl.style.fontSize = `${typingFontSize}px`;
  inputEl.style.fontWeight = typingFontWeight;
  inputEl.style.fontStyle = typingFontStyle;
}

function increaseFontSize() {
  typingFontSize = Math.min(30, typingFontSize + 2);
  applyTypingFontStyle();
}

function decreaseFontSize() {
  typingFontSize = Math.max(16, typingFontSize - 2);
  applyTypingFontStyle();
}

function toggleBold() {
  typingFontWeight = typingFontWeight === "700" ? "400" : "700";
  applyTypingFontStyle();
}

function toggleItalic() {
  typingFontStyle = typingFontStyle === "italic" ? "normal" : "italic";
  applyTypingFontStyle();
}

function resetFontStyle() {
  typingFontSize = 20;
  typingFontWeight = "400";
  typingFontStyle = "normal";
  applyTypingFontStyle();
}

function toggleTheme() {
  const body = document.body;
  const newTheme = body.getAttribute("data-theme") === "light" ? "dark" : "light";
  body.setAttribute("data-theme", newTheme);
}

function setMode(mode) {
  currentMode = mode;
  document.getElementById('btn-practice').classList.toggle('active', mode === 'practicing');
  const examButton = document.getElementById('btn-exam');
  if (examButton) {
    examButton.classList.toggle('active', mode === 'exam');
  }

  const backspaceSelect = document.getElementById('backspace-select');
  const timeSelect = document.getElementById('time-select');
  const levelSelect = document.getElementById('level-select');

  if (mode === 'exam') {
    if (backspaceSelect) {
      backspaceSelect.value = 'off';
      backspaceSelect.disabled = true;
    }
    if (timeSelect) {
      timeSelect.value = '120';
    }
    if (levelSelect) {
      levelSelect.value = 'advance';
    }
    currentLevel = 'advance';
    populateParaSelect();
  } else if (backspaceSelect) {
    backspaceSelect.disabled = false;
  }
  resetSession();
}

function setLevel(level) {
  currentLevel = level;
  populateParaSelect();
  resetSession();
}

function populateParaSelect() {
  const select = document.getElementById('para-select');
  select.innerHTML = '<option value="random">-- Random Paragraph --</option>';
  content[currentLevel].forEach((p, index) => {
    const opt = document.createElement('option');
    opt.value = index;
    opt.textContent = `Paragraph ${index + 1}: ${p.substring(0, 20)}...`;
    select.appendChild(opt);
  });
}

function loadSpecificPara() {
  const idx = document.getElementById('para-select').value;
  if(idx === "random") {
    const pool = content[currentLevel];
    targetText = pool[Math.floor(Math.random() * pool.length)];
  } else {
    targetText = content[currentLevel][idx];
  }
  applyWordLimit();
  renderText();
}

function applyWordLimit() {
  const limit = parseInt(document.getElementById('word-limit').value);
  if (limit > 0) {
    const words = targetText.split(/\s+/);
    targetText = words.slice(0, limit).join(" ");
  }
}

function renderText() {
  displayEl.innerHTML = targetText.split("").map(char => `<span>${char}</span>`).join("");
  displayEl.scrollTop = 0;
  lastOffsetTop = 0;
}

function resetSession(forceFreshParagraph = false) {
  clearInterval(timerInterval);
  startTime = null;
  errors = 0;
  currentWordIndex = 0;
  backspaceCount = 0;
  correctWordsCount = 0;
  incorrectWordsCount = 0;
  totalWordsCount = 0;
  inputEl.value = "";
  inputEl.disabled = false;
  wpmEl.innerText = "0";
  accuracyEl.innerText = "100";

  const selectedTime = parseInt(document.getElementById('time-select').value);
  timeLeft = selectedTime;
  initialTimeLimit = selectedTime;
  timerEl.innerText = selectedTime > 0 ? formatTime(timeLeft) : "--";

  if (forceFreshParagraph) {
    const paraSelect = document.getElementById('para-select');
    if (paraSelect) {
      paraSelect.value = 'random';
    }
  }

  loadSpecificPara();
  updateRings();
}

function formatTime(s) {
  const mins = Math.floor(s / 60);
  const secs = s % 60;
  return `${mins}:${secs.toString().padStart(2, '0')}`;
}

inputEl.addEventListener('keydown', (e) => {
  if (document.getElementById('backspace-select').value === 'off' && e.key === 'Backspace') {
    e.preventDefault();
    return;
  }

  if (e.key === 'Backspace' && document.getElementById('backspace-select').value === 'on') {
    backspaceCount += 1;
  }
});

inputEl.addEventListener("input", () => {
  const val = inputEl.value;
  const targetChars = targetText.split("");
  const spans = displayEl.querySelectorAll("span");
  currentWordIndex = val.length;

  if (!startTime && val.length > 0) {
    startTime = new Date();
    if (timeLeft > 0) {
      timerInterval = setInterval(updateTimer, 1000);
    } else {
      timerInterval = setInterval(updateStatsOnly, 1000);
    }
  }

  errors = 0;
  spans.forEach((span, i) => {
    const typed = val[i];
    if (typed == null) {
      span.className = "";
      if (i === val.length) {
        span.className = "char-current";

        if (span.offsetTop !== lastOffsetTop) {
          displayEl.scrollTo({
            top: span.offsetTop - 60,
            behavior: 'smooth'
          });
          lastOffsetTop = span.offsetTop;
        }
      }
    } else if (typed === targetChars[i]) {
      span.className = "char-correct";
    } else {
      span.className = "char-wrong";
      errors++;
    }
  });

  const accuracy = Math.max(0, Math.round(((val.length - errors) / val.length) * 100)) || 100;
  accuracyEl.innerText = accuracy;
  updateRings();

  if (val.length >= targetText.length) finishSession();
});

function updateTimer() {
  timeLeft--;
  timerEl.innerText = formatTime(timeLeft);
  updateStatsOnly();
  updateRings();
  if (timeLeft <= 0) finishSession();
}

function updateStatsOnly() {
  const timeElapsed = (new Date() - startTime) / 60000;
  const words = inputEl.value.length / 5;
  if (timeElapsed > 0) {
    wpmEl.innerText = Math.round(words / timeElapsed);
  }
  updateRings();
}

function finishSession() {
  clearInterval(timerInterval);
  inputEl.disabled = true;
  const summary = buildResultSummary();
  saveHistory(summary);
  showResultDashboard(summary);
}

function buildResultSummary() {
  const elapsedSeconds = startTime ? Math.max(1, Math.round((new Date() - startTime) / 1000)) : 0;
  const typedText = inputEl.value.trim();
  const typedWords = typedText ? typedText.split(/\s+/) : [];
  const targetWords = targetText.trim() ? targetText.trim().split(/\s+/) : [];
  const totalWords = targetWords.length;

  let correctWords = 0;
  for (let index = 0; index < totalWords; index += 1) {
    if (typedWords[index] && typedWords[index] === targetWords[index]) {
      correctWords += 1;
    }
  }

  const incorrectWords = Math.max(0, totalWords - correctWords) + Math.max(0, typedWords.length - totalWords);
  const accuracy = parseInt(accuracyEl.innerText, 10) || 100;
  const minutesElapsed = elapsedSeconds / 60;
  const grossSpeed = minutesElapsed > 0 ? Math.round(typedWords.length / minutesElapsed) : 0;
  const netSpeed = Math.max(0, Math.round(grossSpeed * (accuracy / 100)));
  const errorRate = totalWords > 0 ? Math.round((incorrectWords / totalWords) * 100) : 0;
  const percentage = accuracy;
  const typingGrade = getTypingGrade(accuracy, netSpeed);
  const performanceMessage = getPerformanceMessage(accuracy, netSpeed);
  const backspaceUsage = document.getElementById('backspace-select').value === 'off'
    ? 'No (Disabled)'
    : `${backspaceCount}`;

  correctWordsCount = correctWords;
  incorrectWordsCount = incorrectWords;
  totalWordsCount = totalWords;

  return {
    netSpeed,
    grossSpeed,
    accuracy,
    correctWords,
    incorrectWords,
    totalWords,
    errorRate,
    timeTaken: formatElapsedTime(elapsedSeconds),
    wpm: parseInt(wpmEl.innerText, 10) || 0,
    percentage,
    typingGrade,
    performanceMessage,
    backspaceUsage,
    modeLabel: currentMode === 'practicing' ? 'Practice' : currentMode === 'exam' ? 'Exam' : 'Practice',
  };
}

function getTypingGrade(accuracy, netSpeed) {
  if (accuracy >= 98 && netSpeed >= 70) return "A+";
  if (accuracy >= 94 && netSpeed >= 55) return "A";
  if (accuracy >= 88 && netSpeed >= 40) return "B";
  return "C";
}

function getPerformanceMessage(accuracy, netSpeed) {
  if (accuracy >= 98 && netSpeed >= 70) return "Outstanding control and exam-ready speed.";
  if (accuracy >= 94 && netSpeed >= 55) return "Excellent balance of speed and precision.";
  if (accuracy >= 88 && netSpeed >= 40) return "Solid performance with room to improve pace.";
  return "Focus on accuracy first, then build speed gradually.";
}

function formatElapsedTime(seconds) {
  const minutes = Math.floor(seconds / 60);
  const remainingSeconds = seconds % 60;
  return `${minutes}:${remainingSeconds.toString().padStart(2, '0')}`;
}

function showResultDashboard(summary) {
  document.getElementById("result-net-speed").innerText = summary.netSpeed;
  document.getElementById("result-gross-speed").innerText = summary.grossSpeed;
  document.getElementById("result-correct-words").innerText = summary.correctWords;
  document.getElementById("result-incorrect-words").innerText = summary.incorrectWords;
  document.getElementById("result-total-words").innerText = summary.totalWords;
  document.getElementById("result-error-rate").innerText = `${summary.errorRate}%`;
  document.getElementById("result-time-taken").innerText = summary.timeTaken;
  document.getElementById("result-wpm").innerText = summary.wpm;
  document.getElementById("result-percentage").innerText = `${summary.percentage}%`;
  const backspaceValueEl = document.getElementById("result-backspace");
  if (backspaceValueEl) {
    backspaceValueEl.innerText = summary.backspaceUsage;
  }
  document.getElementById("result-grade").innerText = `Typing Grade: ${summary.typingGrade}`;
  document.getElementById("result-message").innerText = summary.performanceMessage;
  document.getElementById("result-accuracy-value").innerText = `${summary.accuracy}%`;
  document.getElementById("performance-badge").innerText = summary.typingGrade;
  setRingProgress("result-accuracy", summary.accuracy);
  showScreen("result");
  requestAnimationFrame(() => {
    resultScreenEl.scrollIntoView({ behavior: "smooth", block: "center" });
  });
}

function updateRings() {
  const currentAccuracy = parseInt(accuracyEl.innerText, 10) || 0;
  const currentWpm = parseInt(wpmEl.innerText, 10) || 0;
  const speedProgress = Math.min(100, Math.round((currentWpm / 120) * 100));
  const timeProgress = initialTimeLimit > 0 ? Math.max(0, Math.round((timeLeft / initialTimeLimit) * 100)) : 0;

  setRingProgress("wpm", speedProgress);
  setRingProgress("time", timeProgress);
  setRingProgress("accuracy", currentAccuracy);
}

function setRingProgress(ringName, progressValue) {
  const ringElement = document.querySelector(`[data-ring="${ringName}"]`);
  if (!ringElement) return;
  ringElement.style.setProperty("--progress", Math.max(0, Math.min(100, progressValue)));
}

function retakeTest() {
  resetSession(true);
  showTyping();
  inputEl.focus();
}

function newParagraph() {
  document.getElementById('para-select').value = 'random';
  resetSession(true);
  showTyping();
  inputEl.focus();
}

function backToHome() {
  clearInterval(timerInterval);
  inputEl.disabled = false;
  showSetup();
  window.scrollTo({ top: 0, behavior: "smooth" });
}

function downloadResult() {
  const summary = buildResultSummary();
  const report = [
    "Hindi Typing Master Result",
    `Net Speed: ${summary.netSpeed}`,
    `Gross Speed: ${summary.grossSpeed}`,
    `Accuracy: ${summary.accuracy}%`,
    `Correct Words: ${summary.correctWords}`,
    `Incorrect Words: ${summary.incorrectWords}`,
    `Total Words: ${summary.totalWords}`,
    `Error Rate: ${summary.errorRate}%`,
    `Time Taken: ${summary.timeTaken}`,
    `WPM: ${summary.wpm}`,
    `Percentage: ${summary.percentage}%`,
    `Grade: ${summary.typingGrade}`,
    `Message: ${summary.performanceMessage}`,
  ].join("\n");

  const blob = new Blob([report], { type: "text/plain;charset=utf-8" });
  const url = URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = url;
  anchor.download = "hindi-typing-result.txt";
  anchor.click();
  URL.revokeObjectURL(url);
}

function saveHistory(summary) {
  const userName = document.getElementById('student-name').value.trim() || 'Anonymous';
  const history = JSON.parse(localStorage.getItem("hindi_typing_history") || "[]");
  history.push({
    userName: userName,
    date: new Date().toLocaleString(),
    mode: summary.modeLabel,
    level: currentLevel,
    duration: summary.timeTaken,
    wpm: summary.wpm,
    grossSpeed: summary.grossSpeed,
    netSpeed: summary.netSpeed,
    acc: summary.accuracy,
    correctWords: summary.correctWords,
    incorrectWords: summary.incorrectWords,
    totalWords: summary.totalWords,
    errorRate: summary.errorRate,
    backspaceUsed: summary.backspaceUsage,
  });
  localStorage.setItem("hindi_typing_history", JSON.stringify(history));
  updateHistoryTable();
}

function updateHistoryTable() {
  const history = JSON.parse(localStorage.getItem("hindi_typing_history") || "[]");
  document.getElementById('history-body').innerHTML = history.reverse().slice(0, 10).map(row => `
    <tr>
      <td>${row.date}</td>
      <td>${row.userName || 'Anonymous'}</td>
      <td>${row.mode || 'Practice'}</td>
      <td>${row.level.toUpperCase()}</td>
      <td>${row.duration || '--'}</td>
      <td>${row.wpm}</td>
      <td>${row.grossSpeed ?? '--'}</td>
      <td>${row.netSpeed ?? '--'}</td>
      <td>${row.acc}%</td>
      <td>${row.correctWords ?? 0}</td>
      <td>${row.incorrectWords ?? 0}</td>
      <td>${row.totalWords ?? 0}</td>
      <td>${row.errorRate ?? '--'}%</td>
      <td>${row.backspaceUsed || '0'}</td>
    </tr>
  `).join("");
}

function clearHistory() {
  localStorage.removeItem("hindi_typing_history");
  updateHistoryTable();
}

init();

document.getElementById("setup-screen").classList.add("is-visible");